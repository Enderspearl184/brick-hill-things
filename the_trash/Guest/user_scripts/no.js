/*
Game.command("killbricks", (p)=>{
	world.bricks.forEach((brick)=>{
		new PacketBuilder("Brick")
		.write("uint32", brick.netId)
		.write("string", "kill")
		.broadcast()
		console.log("packet!!!")
	})
})
*/